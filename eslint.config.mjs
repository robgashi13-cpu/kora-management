export default [
  {
    ignores: ['dist/**', 'build/**', 'node_modules/**', 'legacy_src/**', 'app/**', 'ios/**', '.next/**'],
    rules: {
      'no-unused-vars': 'off',
      'no-undef': 'off'
    }
  }
];
